import { Pipe } from '@angular/core';
var NzOptionPipe = (function () {
    function NzOptionPipe() {
    }
    NzOptionPipe.prototype.transform = function (options, value) {
        if (value.searchText) {
            var _options = options.filter(function (option) { return option.nzLabel && (option.nzLabel.toLowerCase().indexOf(value.searchText.toLowerCase()) !== -1); });
            if (value.tags) {
                _options = options.filter(function (option) { return option.nzLabel && (option.nzLabel.toLowerCase() === value.searchText.toLowerCase()); });
            }
            if (_options.length) {
                return _options;
            }
            else {
                return [{
                        nzValue: value.value,
                        _value: value.value,
                        nzDisabled: value.disabled,
                        _disabled: value.disabled,
                        nzLabel: value.notFoundContent,
                        _label: value.notFoundContent,
                    }];
            }
        }
        else {
            return options;
        }
    };
    return NzOptionPipe;
}());
export { NzOptionPipe };
NzOptionPipe.decorators = [
    { type: Pipe, args: [{ name: 'nzOptionPipe' },] },
];
/** @nocollapse */
NzOptionPipe.ctorParameters = function () { return []; };
