import { Pipe } from '@angular/core';
import * as moment from 'moment';
var NzDatePipe = (function () {
    function NzDatePipe() {
    }
    NzDatePipe.prototype.transform = function (value, formatString) {
        if (value) {
            return moment(+value).format(formatString);
        }
        else {
            return '';
        }
    };
    return NzDatePipe;
}());
export { NzDatePipe };
NzDatePipe.decorators = [
    { type: Pipe, args: [{ name: 'nzDate' },] },
];
/** @nocollapse */
NzDatePipe.ctorParameters = function () { return []; };
